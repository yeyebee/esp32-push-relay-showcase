# ESP32 -> /ingest 호출에 사용되는 공유 비밀
API_KEY=change-me-to-a-long-random-string

# Firebase 서비스 계정 JSON 경로 (절대경로 권장)
FIREBASE_CREDENTIALS_PATH=/home/deploy/secrets/firebase-adminsdk.json

# SQLite DB 파일 경로
DATABASE_PATH=./data/devices.db
